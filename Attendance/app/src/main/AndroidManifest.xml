<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.attendance">

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:name=".global"
        android:allowBackup="true"
        android:icon="@drawable/temp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/temp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <uses-library android:name="org.apache.http.legacy" android:required="false" />
        <activity
            android:name=".student.studentIndex"
            android:label="@string/title_activity_student_index"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.CameraAttandance" />
        <activity android:name=".activity.forgetpassword" />
        <activity android:name=".activity.selectrollnumber" />
        <activity android:name=".setpassword" />
        <activity
            android:name=".index"
            android:label="@string/title_activity_index"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.login" />
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<uses-library-->
            <!--android:name="org.apache.http.legacy"-->
            <!--android:required="false" />-->
    </application>
</manifest>